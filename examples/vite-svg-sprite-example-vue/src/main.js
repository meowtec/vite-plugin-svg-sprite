import { createApp } from 'vue'
import App from './App.vue'
import './index.css'

// const modules = import.meta.glob('./assets/icons/delay/*.svg')
// Object.entries(modules).forEach(async ([key, el]) => {
//     await el()
// })

createApp(App).mount('#app')
